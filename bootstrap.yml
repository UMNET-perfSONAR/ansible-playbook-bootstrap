# bootstrap.yml

- name: bootstrap bare-metal
  hosts:
    - bare-metal
  roles:
    - ansible-role-bootstrap-dell

- name: bootstrap MiServer
  hosts:
    - miserver
  roles:
    - ansible-role-bootstrap-miserver
